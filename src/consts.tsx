// import { firebase } from './firebase/config';

export const USER_ID = "cheryllaird";
// export const USER_ID = firebase.auth().currentUser.uid;

export const PAGES = {
    MENU: "On the Menu",
    SHOPPING: "Shopping List",
    RECIPES: "Recipes",
};

export const STORAGE_KEYS = {
    MENU: "MealPlan",
};
